version: '3.8'

services:
  agent:
    build: .
    container_name: lead_to_cash_agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./checkpoints_data:/app/checkpoints_data
    restart: unless-stopped